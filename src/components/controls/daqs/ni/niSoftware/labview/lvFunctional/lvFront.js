import React from "react";

const LVFront = () => {
  return (
    <div>
      <h1> LabView Front</h1>
      <p>
        The front panel is where the graphical user interface lives. This is
        where the user can see 2 types of objects. Controls are objects that can
        put inoput into the ruinning loop. Indicators are just output values
        from inside the loop
      </p>
    </div>
  );
};
export default LVFront;
