import React from 'react';

const CSSNotes = () => {
  return (
    <div>
      <h2>CSS Notes</h2>
        <li>CSS is used to style the HTML, make it pretty</li>
        <li>It consist of selectors, properties and values.  The property and the value to gether make a declaration, the whole thing together makes a ruleset</li>
        <li>You can oput multiple elements in the selectors by putting commas in-between ex.  p, h1, h2</li>
        <li>The CSS sheet has to be linked to the html page by the l;ink tag.</li>
        <li>Use /* */  for comments in CSS</li>
        <a href='https://developer.mozilla.org/en-US/docs/Learn/Getting_started_with_the_web/CSS_basics'>Mozilla CSS Basics</a>
    </div>
  )
}

export default CSSNotes;